.DS_Store

/target

# Transient task worktrees — created/destroyed by WorktreeManager at runtime.
# Each task gets an isolated checkout at .worktrees/{task_id}/.
# .tasks/ is NOT listed here because it is committed (it holds task artifacts).
/.worktrees/
