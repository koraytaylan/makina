# Plan 0001 — Todo Core (Sequential) — 📋 Planned

The roll-up row in [../STATUS.md](../STATUS.md) must stay in sync with this file. Task-level truth lives in [tasks/](tasks/) frontmatter; Makina's integration coordinator updates both layers.

- **Status:** 📋 Planned.

- **Goal:** demonstrate a strictly sequential Makina run with three small, verifiable Rust edits to `src/main.rs`.
- **Root cause:** the starter `Task` only has a constructor and a printer; there is no behavior to chain.
- **Approach:** add `toggle`, then `count_open` (depending on `toggle`), then `print_summary` (depending on `count_open`).
- **Progress:** 0/3 tasks done; 0 blocked; 0 dropped.
- **Integration:** `planned`; run —; base `develop` @ `{{BASE_OID}}`; validation base —; mode —; final integration —.
- **Exceptions:** — (coordinator-owned blocked/dropped reasons are recorded here).
- **Outcome:** The starter project gains a toggle, an open-task counter, and a summary printer, each landed in order, while all configured gates remain green.

_Last updated: 2026-07-20, against `develop` @ `{{BASE_SHORT_OID}}`._