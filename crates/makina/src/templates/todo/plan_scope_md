# Scope — Plan 0001

> Add a small, verifiable state transition to the runnable todo starter.

## Why this plan

The starter demonstrates Makina's per-task plan workflow with one bounded Rust change.

## In scope

- **0001 — Todo Core.** Add and test a method that toggles a task's completion state.

## Out of scope

- Persistence, networking, and changes outside `src/main.rs`.
