# Plan 0001 — Todo Starter — 📋 Planned

The roll-up row in [../STATUS.md](../STATUS.md) must stay in sync with this file. Task-level truth lives in [tasks/](tasks/) frontmatter; Makina's integration coordinator updates both layers.

- **Status:** 📋 Planned.

- **Goal:** demonstrate a complete per-task Makina plan with one small, verifiable Rust change.
- **Root cause:** the starter task type has no operation that changes its completion state.
- **Approach:** add and test `Task::toggle` within the single authored footprint.
- **Progress:** 0/1 tasks done; 0 blocked; 0 dropped.
- **Integration:** `planned`; run —; base `develop` @ `{{BASE_OID}}`; validation base —; mode —; final integration —.
- **Exceptions:** — (coordinator-owned blocked/dropped reasons are recorded here).
- **Outcome:** The starter project gains a tested task-state toggle while all configured gates remain green.

_Last updated: 2026-07-20, against `develop` @ `{{BASE_SHORT_OID}}`._
