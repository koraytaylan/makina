# Files exempt from the coverage gate enforced by check_coverage.ts.
# Each line is either a full repo-relative path or a basename. Comments
# start with #. Add justifications inline so future maintainers (and code
# reviewers) understand why a file is exempt.
#
# main.ts — argv-dispatch shell at packages/cli/main.ts. The substantive
# logic lives in @makina/core (packages/core/src/) where it is fully
# covered; the CLI entry point itself is just wiring. The exclude pattern
# matches any path ending in "/main.ts", which today resolves only to the
# CLI entry point.
main.ts
